version: '2'
linters:
  default: all
  disable:
    - nlreturn
    - noinlineerr
    - varnamelen
    - wsl_v5
    # retire
    - exhaustruct
    - gomodguard
    - wsl
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    paths:
      - third_party$
      - builtin$
      - examples$
    rules:
      - path: _test\.go
        linters:
          - goconst
  settings:
    depguard:
      rules:
        main:
          allow:
            - $gostd
            - 'golang.org/x/sys'
            - 'golang.org/x/text'
            - 'github.com/google/uuid'
            - 'github.com/nalgeon/be'
            - 'github.com/Defacto2/helper'
formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
